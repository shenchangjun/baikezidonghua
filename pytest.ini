[pytest]
# 测试根目录与收集路径
testpaths = testcase
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Allure 结果输出目录 + 控制台摘要（-ra 显示跳过/失败/通过摘要）
addopts = --alluredir=allure-results -ra
